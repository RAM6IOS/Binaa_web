# AGENTS.md — معايير المشروع الإلزامية

## قاعدة الاستجابة (Responsive)

تُطبَّق على كل صفحة تُبنى في هذا المشروع:

### 1. القائمة الجانبية (AppLayout المكوّن المشترك)
- **سطح المكتب/التابلت الأفقي (>1024px):** ثابتة ظاهرة كما هي.
- **الموبايل (<768px):** مخفية افتراضياً، تظهر كقائمة منسدلة (Drawer) من اليمين (RTL) عبر أيقونة قائمة (☰) في الأعلى، مع طبقة تعتيم خلفية.
- تُبنى مرة واحدة فقط داخل `AppLayout`، فيرثها تلقائياً كل صفحة دون كود إضافي.

### 2. الجداول
- **سطح المكتب/التابلت:** جدول تقليدي كما هو مصمَّم.
- **الموبايل:** كل صف يتحوّل تلقائياً إلى بطاقة عمودية مصغّرة (Card) بدل التمرير الأفقي — أهم حقلين (مثل الاسم والحالة) بخط بارز أعلى البطاقة، البقية أسفلها بخط أصغر.

### 3. النوافذ الجانبية (Side panels للإضافة/التعديل)
- **سطح المكتب:** عرض ثابت 35-40% كما اعتُمد.
- **الموبايل:** تتحوّل لتغطية الشاشة بالكامل تقريباً (90-100%).

### 4. اختبار إلزامي قبل تسليم أي صفحة
التحقق بصرياً على ثلاثة أعراض محددة:
- **375px** — هاتف
- **768px** — تابلت
- **1440px** — سطح مكتب

لا تُسلَّم أي صفحة كـ"جاهزة" دون هذا التحقق الثلاثي.

---

## قواعد التصميم الثابتة (Design System) — إلزامية لطول المشروع

المصدر الوحيد للقواعد: `.opencode/references/design-system.md` — **اقرؤه أولاً قبل أي عمل UI**.

**سياق المستخدم (يُفرض على كل قرار تصميمي):** النظام يُشغَّل في الميدان — موقع إنشاءات/أشغال عمومية — من قبل عمال ومهندسين، بإضاءة خارجية قوية، وربما قفازات، وشبكة ضعيفة أو منقطعة، على هواتف تُستعمل بيد واحدة.

### قواعد ممنوعة/مُلزمة
- **ممنوع** كتابة ألوان خام (hex/`slate-*`/`blue-*`…) في الصفحات — الألوان من الـ design tokens فقط.
- **ممنوع** هرمية خطوط أو مسافات أو زوايا أو ظلال مخصصة خارج المقياس الموحّد.
- **ممنوع** إعادة إنشاء ترويسة صفحة/غلاف/حالات بيانات يدوياً — استخدم المكوّنات المشتركة:
  - `src/components/layout/PageContainer.tsx` (غلاف الصفحة)
  - `src/components/layout/PageHeader.tsx` (الترويسة)
  - `src/components/ui/data-state.tsx` (تحميل / خطأ / فارغ)
- **ممنوع** إشارة حالة بلون فقط — **دائماً لون + أيقونة + نص** (وضوح تحت الشمس والقفازات).
- **مُلزم** في كل صفحة بيانات: حالة تحميل + حالة خطأ + **زر إعادة محاولة** (الشبكة في الموقع قد تقطع).
- **مُلزم** أن تكون رسائل الخطأ والفارغ واضحة باللغة الحالية (AR/FR)، لا نصوص تقنية.

### فحص التسليم
- [ ] قرأت `design-system.md`
- [ ] يستخدم `PageContainer` + `PageHeader` + `DataState`
- [ ] لا ألوان/أخطاط/مسافات مخصصة خارج tokens/scale
- [ ] `npm run check:design` نظيف (السكربت الحارس — إلزامي قبل أي تسليم)
- [ ] رُوفق بين 375px / 768px / 1440px

---

## شروط إلزامية لكل كود يُكتب في المشروع

### 1. الأمان أولاً، لا كإضافة لاحقة
- افترض دائماً أن هذا تطبيق متعدد المستأجرين (Multi-tenant) — كل استعلام بيانات يجب أن يمر عبر RLS، لا فحصاً يدوياً في كود التطبيق فقط.
- تحقق من كل مدخل مستخدم قبل استخدامه، ولا تثق بأي قيمة قادمة من المتصفح دون اشتقاقها من الجلسة الموثّقة في الخادم.
- لأي عملية حساسة (كلمة مرور، صلاحيات، حذف)، اشرح السيناريوهات التي قد يُساء استخدامها فيها **قبل** كتابة الكود، لا بعده.

### 2. كود احترافي قابل للصيانة
- لا تكرر منطقاً موجوداً في مكوّن أو دالة أخرى — أنشئ دالة/مكوّناً مشتركاً قابلاً لإعادة الاستخدام.
- اسمِ المتغيرات والدوال بوضوح يشرح الغرض، لا اختصارات غامضة.
- أضف تعليقاً موجزاً فقط عند المنطق غير البديهي (لماذا تحقق من كذا؟)، لا تعليقاً يشرح ما هو واضح من الكود نفسه.
- افترض أن مطوراً آخر سيقرأ هذا الكود دون سياق إضافي — هل يفهمه بسرعة؟

### 3. لا تتجاوز نطاق الطلب
- لا تقترح أو تبدأ ببناء أي ميزة أو صفحة إضافية لم تُطلب صراحة، حتى لو بدت "خطوة منطقية تالية".
- إن رأيت أن الطلب يفتقد شيئاً ضرورياً لعمله بشكل صحيح، أخبرني بذلك أولاً كسؤال، ولا تضِفه من تلقاء نفسك.

### 4. الشفافية قبل التسليم
- **قبل** كتابة أي كود: اشرح الخطط — الملفات التي ستُنشئها أو تعدّلها، والجداول أو السياسات المتأثرة.
- **بعد** الكتابة: أعطني قائمة منفصلة بأكثر النقاط حساسية في هذا الكود تحديداً والتي يجب أن أراجعها شخصياً قبل القبول.

---

## مهارات Agent Skills المثبّتة (OpenCode)

هذا المشروع يستخدم مهارات مثبّتة تحت `.opencode/skills/` (ومراجعها المشتركة في `.opencode/references/`).

### القاعدة الذهبية: لا تنفيذ بدون مهارة
- **ممنوع** كتابة أي سطر كود أو تعديل ملف قبل تحميل المهارة المناسبة عبر أداة `skill`.
- **ممنوع** تخطي أداة `skill` بحججة "المهمة بسيطة" أو "أعرف الحل".
- **لا استثناء:** حتى لو بدت المهمة تافهة، يجب تحميل مهارة أو التأكيد كتابياً أنها لا تحتاج مهارة.

### القواعد الأساسية
- إذا تطابقت المهمة مع أي مهارة، استدعِها عبر أداة `skill` قبل أي تنفيذ.
- اتبع سير عمل المهارة حرفياً ولا تطبّقه جزئياً.
- لا تتجاوز الخطوات الإلزامية (spec / plan / test) عندما تفرضها مهارة فعّالة.

### خريطة «نية المهمة ← المهارة»
- ميزة جديدة / وظيفة → `spec-driven-development` ثم `incremental-implementation` مع `test-driven-development`
- إعادة هيكلة / تبسيط كود → `code-simplification`
- تصميم API أو واجهة → `api-and-interface-design`
- عمل UI → `frontend-ui-engineering`
- باغ / فشل / سلوك غير متوقع → `debugging-and-error-recovery`
- مراجعة كود → `code-review-and-quality`
- تخطيط وتقسيم عمل → `planning-and-task-breakdown`
- أمان / مدخلات مستخدم → `security-and-hardening`
- أداء → `performance-optimization`
- نشر / إطلاق → `shipping-and-launch`
- طلب غير واضح المعالم → `interview-me` أو `idea-refine`

### نموذج التنفيذ الإلزامي (لكل طلب)
1. **قبل أي عمل:** استخدم أداة `skill` لتحميل المهارة المناسبة.
2. **إذا لم تنطبق أي مهارة:** اكتب بوضوح: "لا توجد مهارة مناسبة لهذه المهمة" مع التبرير.
3. **نفّذ** سير العمل كما هو محدّد في المهارة.
4. **لا تنتقل** إلى التنفيذ الفعلي قبل اكتمال الخطوات المطلوبة في المهارة.

---

### التحقق من تحميل المهارات
- قبل تسليم أي عمل، تأكد أنك استخدمت أداة `skill` على الأقل مرة واحدة.
- إذا كتبت كوداً بدون تحميل مهارة، يعتبر ذلك انتهاكاً للقواعد.
- السؤال الذي يجب أن تسأله لنفسك: "هل حمّلت مهارة قبل هذا الكود؟"
